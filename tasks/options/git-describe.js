module.exports = {
  "options": {
    prop: 'GIT_COMMIT',
    //Fail siliently if running the server in dev mode
    failOnError: false,//grunt.config("currentEnvironment") == 'production'
  },
  "describe": {
    // Target-specific file lists and/or options go here.
  }
}
